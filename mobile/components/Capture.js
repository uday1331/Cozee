import React from 'react';
import { View } from 'react-native';
import { Ionicons, Entypo } from '@expo/vector-icons';

const Capture = () => {
    return(
        <View style={{ flex: 1 }}>
            <Entypo name="circle" size={86} color="white" />
        </View>
    );
}

export default Capture;